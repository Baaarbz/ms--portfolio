spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/portfolio
    username: local
    password: local
    driver-class-name: org.postgresql.Driver

logging:
  level:
    dev.eduardobarbosa: DEBUG
    web: DEBUG

dev:
  eduardobarbosa:
    auth:
      usr: local
      pwd: local